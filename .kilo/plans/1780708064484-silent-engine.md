# Fix Plan for GMCQ Quiz Engine

## Issue Verification

### 1. Remove PII from Plugin Header (gmcq-core.php:8-9) ✅ VALID
- Current code has: `Created @SparkzDev (+91 7530966985)` on line 9
- Fix: Remove phone number, keep `Created @SparkzDev`

### 2. Fix gmcq_render_single_quiz_template (class-gmcq-frontend.php:285-307) ✅ VALID
- Current code uses `add_action('the_content', ...)` but function echoes output
- Currently returns void on non-quiz pages (would return nothing, breaking other content)
- Fix: Change to `add_filter`, accept `$content` parameter, return the quiz output replaced with original content on non-quiz pages

### 3. Fix Quiz Archive Link URLs (class-gmcq-frontend.php:164-169) ✅ VALID
- Current code has: `home_url( get_post_field( 'post_name', $quiz->quiz_id ) )` on line 166
- This produces wrong URLs like `/ssb-mts/` instead of `/quiz/ssb-mts/`
- Fix: Use `get_permalink($quiz->quiz_id)` for proper CPT URLs

## Changes Required

**File 1: gmcq-core.php**
- Line 9: Change `Created @SparkzDev (+91 7530966985)` to `Created @SparkzDev`

**File 2: class-gmcq-frontend.php**
- Lines 285-307: Rewrite `gmcq_render_single_quiz_template` function to:
  - Accept `$content` parameter
  - Return original `$content` if not singular quiz
  - Return quiz HTML when is singular quiz
  - Change `add_action` to `add_filter` on line 307

- Line 166: Change `home_url( get_post_field( 'post_name', $quiz->quiz_id ) )` to `get_permalink( $quiz->quiz_id )`